
class bareos::params {

    $package_filedaemon = 'bareos-filedaemon'
    $package_bconsole   = 'bareos-bconsole'
    $package_director   = 'bareos-director'
    $package_storage    = 'bareos-storage'
    $package_webui      = 'bareos-webui'
    $package_db_mysql   = 'bareos-database-mysql'
    $package_db_pgsql   = 'bareos-database-mysql'
    $package_db_sqlite  = 'bareos-database-mysql'
    
    $filedaemon_conf = '/etc/bareos/bareos-fd.conf'

}
