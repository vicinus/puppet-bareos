Storage {
  Name = "<%= @storage_name %>"
  Plugin Directory = "<%= @plugins_dir %>"
  Plugin Names = "<%= @plugins.join(' ') %>"
  <%- @options.each do |(name, value)| -%>
  <%= name %> = <%= value %>
  <%- end -%>
}
