Job {
    Name = "<%= @job_name %>"
    Type = <%= @job_type %>
    Pool = <%= @pool %>
    Messages = <%= @messages %>
    <%- @options.each do |(name, value)| -%>
    <%= name %> = <%= (value.kind_of? String) ? '"' + value + '"' : value %>
    <%- end -%>
}
